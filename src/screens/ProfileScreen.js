import React from 'react';
import { View, Text } from 'react-native';

const ProfileScreen = ({ navigation, route }) => {
  return <Text style={{ color: 'white' }}>This is profile</Text>;
};

export default ProfileScreen;
